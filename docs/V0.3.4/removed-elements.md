# Removed elements V0.3.4

No user-facing feature was removed in this iteration.

The implementation only replaces brittle per-icon positioning with centered icon wrappers and adjusts several coordinates in existing view components.
