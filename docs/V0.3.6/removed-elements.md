# Removed elements

No user-facing feature was removed in V0.3.6.

The changes only replace fragile visual positioning with safer local component layouts. The standalone UV alert card remains absent because the alert is now intentionally embedded inside the UV card to match the requested interaction and visual grouping.
