# V0.3.6 removed or replaced elements

No user-facing feature was removed.

The standalone UV alert card was replaced by an inline alert area inside the UV card. This keeps the same information visible while reducing vertical fragmentation and matching the requested layout.

The hourly forecast strip no longer clips the last partially visible hour as a static overflow effect. It now uses a horizontal scroll viewport so future hours remain reachable.
